---
deployment:
  tasks:
    # 1. Di chuyển vào thư mục code (tương đương cd public_html/dalatbds.com/)
    - export DEPLOYPATH=/home/qymxlvghhosting/public_html/dalatbds.com
    
    # (Thao tác git pull origin main đã được cPanel tự động làm ở nền rồi)

    # 2. Chạy migrate (tương đương php artisan migrate --force)
    # Lưu ý: Ở cPanel, mình nên dùng đường dẫn tuyệt đối của PHP (/usr/local/bin/php) để tránh lỗi không tìm thấy lệnh
    - cd $DEPLOYPATH && /usr/local/bin/php artisan migrate --force